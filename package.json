{
  "name": "wavchatbot",
  "version": "1.0.0",
  "description": "Fred Wav Chatbot - Web application with RAG system for content creation expertise",
  "private": true,
  "scripts": {
    "dev": "npm --workspace web run dev",
    "build": "npm --workspace web run build",
    "start": "npm --workspace web run start",
    "lint": "npm --workspace web run lint",
    "ingest": "node scripts/ingest.js"
  },
  "workspaces": [
    "apps/web"
  ],
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "dotenv": "^16.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.38.0",
    "openai": "^4.0.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  }
}